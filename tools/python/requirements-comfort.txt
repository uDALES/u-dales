# PET (MEMI) and official operational UTCI; install with pip install -e 'tools/python[comfort]'.
# pythermalcomfort 4.x requires Python >=3.10. Core uDALES tools still support 3.9.
pythermalcomfort==4.6.0
