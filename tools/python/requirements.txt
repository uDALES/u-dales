# Core dependencies for uDALES Python tools
# Requires Python >= 3.9. Older interpreters such as Python 3.6 do not
# support this codebase (for example, `from __future__ import annotations`).
numpy>=1.20.0
scipy>=1.7.0
pandas>=1.3.0
xarray>=0.19.0
netCDF4>=1.5.0
h5netcdf>=1.0.0
trimesh>=3.9.0
shapely>=2.0.0
triangle
matplotlib>=3.3.0
f90nml>=1.4.4
# numba is only needed for the numba-accelerated direct-shortwave kernels
numba>=0.60.0
# pvlib is only needed for the pvlib solar-position backend
pvlib>=0.13.0
# plotly is only needed for the interactive plotting backend
plotly>=5.0.0

# Notebook support (optional)
ipykernel>=6.0.0
nbformat>=4.2.0

# Optional PyVista/VTK visualization backend: see requirements-pyvista.txt
# (kept separate so the heavy VTK dependency is opt-in).
