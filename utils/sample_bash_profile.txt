export PS1='\[\e]2;\u@\h:$(pwd)\007\e]1;\h\007\]\u@\h:\w>'  # shows hostname

# set environmental variables for dales
export PLATFORM=cx1
export DA_TOPDIR=$HOME/dales-urban
export DA_UTILSDIR=$DA_TOPDIR/utils
export DA_SRCDIR=$DA_TOPDIR/src
export DA_EXPDIR=$DA_TOPDIR/exp
if [ "$(hostname)" == 'cx2-login' ] ; then
        export DA_WORKDIR=$SCRATCH
else
        export DA_WORKDIR=$WORK/dales-urban
fi

export PATH=$PATH:$DA_UTILSDIR  # to call functions in utils from anywhere

read modules < $DA_UTILSDIR/dalesmodules # read in list of modules

# load modules
module load git
module load anaconda3/personal  # this is python
module load udunits  # need to load before nco and ncview
module load nco
module load ncview
# module load modules  # load modules for compiling Dales

# export PATH=$PATH:~\bin    # not sure this is needed?

# shortcuts and settings
alias ll='ls -ahl --color=auto'
alias ls='ls --color=auto'
alias grep='grep -In --color=auto'
alias vi='vim'
alias rm='rm -I'
alias wd='cd $WORK'
alias xd='cd $DA_SRCDIR'