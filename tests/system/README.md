# System Tests

This directory is reserved for whole-code validation cases, including runs that
require substantial compute resources.

Suggested contents:

- `cases/` for system-test simulation setups
- `scripts/` for launcher and post-processing helpers
- reference documentation describing expected runtime, resources, and validation criteria
