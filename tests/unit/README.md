# Unit Tests

This directory is reserved for isolated tests of small routines or modules.

Use this area only when the target can be tested independently with minimal setup.
Larger solver, startup, and MPI-driven checks should remain under `tests/integration` or `tests/regression`.
